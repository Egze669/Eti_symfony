{% extends "blog/index.html.twig" %}
{% block content%}
{#    {% for label, messages in app.flashes %}#}
{#        {% for message in messages %}#}
{#            <div class="flash-{{ label }}">#}
{#                {{ message }}#}
{#            </div>#}
{#        {% endfor %}#}
{#    {% endfor %}#}
    <ul>
        {% for category in blogCategories %}
        <li><a href="{{ url('view',{'id':category.id}) }}">{{ category.name }}</li>
        {% endfor %}
    </ul>
    <p><a href="{{ url('view',{'id':0}) }}">Show all posts</p>
    <p><a href="{{ url('newcat') }}">New category<a href="{{ url('newpost') }}">New post</p>
{% endblock %}
